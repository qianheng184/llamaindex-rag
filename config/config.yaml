# RAG 系统配置文件

# Embedding 模型配置
embedding_model: "BAAI/bge-small-zh-v1.5"

# 文本切分配置
chunk_size: 500
chunk_overlap: 50

# 检索配置
top_k: 3

# LLM 配置
llm_model: "deepseek-chat"

# 路径配置
vector_store_path: "./storage"
data_path: "./data"

# API 配置
api_host: "0.0.0.0"
api_port: 8000
